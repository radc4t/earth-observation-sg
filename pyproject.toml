# Ruff config for the Python asset-generation scripts (scripts/**).
[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
# Ruff's default rules (pyflakes F + pycodestyle E4/E7/E9) plus import sorting (I).
select = ["E4", "E7", "E9", "F", "I"]
